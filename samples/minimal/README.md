## Minimal sample

- `ButtonSelect` is added to as the root node
- `text` is set to `Click`
- 3 items are added to the `items` array in the editor
- Can click through different items and test that clicking outside of the item buttons would hide the buttons
